For error "0x002 Incorrect number of arguments.", we are assuming that arguments do
not contain spaces. So usernames, passwords, directory names, and file names do not
contain spaces. This is because there is no easy way to distinguish between multiple
arguments and a single argument with spaces.
